/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { DetailPropetyResolverService } from './detail-propety-Resolver.service';

describe('Service: DetailPropetyResolver', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [DetailPropetyResolverService]
    });
  });

  it('should ...', inject([DetailPropetyResolverService], (service: DetailPropetyResolverService) => {
    expect(service).toBeTruthy();
  }));
});
